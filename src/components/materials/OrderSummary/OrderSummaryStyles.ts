import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    flex_row: {
        flexDirection: 'row',
        justifyContent: 'space-between'
    },
    ingredients: {
        marginTop: 20
    }
});
