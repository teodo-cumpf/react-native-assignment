import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    wrapper: {
        gap: 10
    },
    flex_row:{
        flexDirection: 'row',
        justifyContent: 'space-between',
    },
    ingredients: {
        paddingLeft: 20
    }
});
