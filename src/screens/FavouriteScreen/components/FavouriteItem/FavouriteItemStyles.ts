import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    wrapper: {
        gap: 20
    },
    button_row: {
        flexDirection: 'row',
        justifyContent: 'space-between'
    },
    button_wrapper: {
        width: "82%"
    }
});
